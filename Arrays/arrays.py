# Accessing values in an array
new_list = [1, 2, 3]
result = new_list[0]

if 1 in new_list: print(True)

for n in new_list:
    if n == 1:
        print(True)
        
    break  

# numbers = []
# len(numbers) 
# numbers.append(2)
# numbers.append(200)
# numbers.extend(4, 5, 6) 


#Linked lists
# A linked list is a linear data structure where each element in a list is contained in a separate object called a node
#The first node-head is the beginning of a list and references the next node while the last is the tail and doesnt reference any node 